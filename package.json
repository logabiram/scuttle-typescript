{
  "name": "scuttle-ts-lib",
  "version": "1.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "start": "rm -rf dist && npm run build && node dist/index.js"
  },
  "devDependencies": {
    "@types/node": "^22.13.12",
    "typescript": "^4.9.5"
  }
}
